# demos

Static, self-contained interactive prototypes. Each lives in its own folder and
is a single HTML file with no build step — dependencies load from a CDN.

| Demo | Path |
| --- | --- |
| PBX Automate | [`pbx-automate/`](pbx-automate/) |

Mock data throughout. Nothing here talks to a live service.
